kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: run-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
      name: runfiles